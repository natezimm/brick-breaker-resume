{
  "name": "brick-breaker-resume",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "generate:resume": "node scripts/generate_resume_json.js",
    "predev": "npm run generate:resume",
    "prebuild": "npm run generate:resume",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "quality": "npm run format:check && npm run lint && npm run test:coverage && npm run test:e2e"
  },
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.23.0",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.60.0",
    "babel-jest": "^29.7.0",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "mammoth": "^1.11.0",
    "prettier": "^3.8.3",
    "vite": "^7.3.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
